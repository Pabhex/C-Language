#include<stdio.h>
int main() {
    int a,b;
    int sum;
    int diff;
    scanf("%d %d",&a,&b);
    sum = a+b;
    diff = a-b;
    printf("the sum of the entered value is : %d\n",sum);
    printf("the diff of entered value is : %d",diff);
    return 0;
}